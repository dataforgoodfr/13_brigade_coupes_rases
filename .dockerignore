# Used when building from repo root, e.g.:
#   docker build -f data_pipeline/Dockerfile -t data-pipeline .
# Host poetry.lock can lag pyproject.toml; the image runs `poetry lock` instead.
data_pipeline/poetry.lock
